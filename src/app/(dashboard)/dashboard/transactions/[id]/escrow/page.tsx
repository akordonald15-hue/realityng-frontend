import { TransactionEscrowClient } from "./transaction-escrow-client";

export const dynamic = "force-dynamic";

export default function TransactionEscrowPage() {
  return <TransactionEscrowClient />;
}
